import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root'
})
export class WcagService {

  bigFont: boolean;
  darkMode: boolean;
  letterSpacing: boolean;
  lineSpacing: boolean;
  constructor(

  ) { }
}
